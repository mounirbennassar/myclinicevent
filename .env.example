# docker-compose settings (copy to .env). Backend settings live in backend/.env.
DOMAIN=event.myclinic.com.sa
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(24))"
POSTGRES_PASSWORD=change-me
